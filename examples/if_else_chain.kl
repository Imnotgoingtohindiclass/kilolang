func sign(int n) -> int {
    if (n > 0) {
        return 1;
    } else if (n < 0) {
        return -1;
    } else {
        return 0;
    }
}

func main() -> int {
    int x = -5;
    while (x <= 5) {
        print(sign(x));
        x = x + 1;
    }
    return 0;
}