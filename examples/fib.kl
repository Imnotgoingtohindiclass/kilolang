func fib(int n) -> int {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

func main() -> int {
    int i = 1;
    while (i <= 8) {
        print(fib(i));
        i = i + 1;
    }
    return 0;
}